{
    "$id": "http://brise.com/selection.schema.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Schemes of the algorithm of selection",
    "description": "Approach for exploration and describing all possible configuration (search space).",

    "definitions": {
        "SobolSequence": {
            "title": "Sobol sequences are an example of quasi-random low-discrepancy sequences.",
            "description": "A Dict with configuration of sobol algorithm.",
            "type": "object",
            "properties": {
                "SelectionType": {
                    "type": "string",
                    "enum": ["SobolSequence"]
                },
                "NumberOfInitialConfigurations": {
                    "description": "The number of configurations that will be tested before making any attempts to build prediction model.",
                    "type": "integer",
                    "minimum": 0
                }
            },
            "required": [
                "SelectionType",
                "NumberOfInitialConfigurations"
            ]
        }
    }
}