{
    "$id": "http://brise.com/selection.schema.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Schemes of the algorithm of selection",
    "description": "Approach for exploration and describing all possible configuration (search space).",

    "definitions": {
        "SobolSequence": {
            "title": "Sobol sequences are an example of quasi-random low-discrepancy sequences.",
            "description": "A Dict with configuration of sobol algorithm.",
            "type": "object",
            "properties": {
                "SelectionType": {
                    "type": "string",
                    "enum": ["SobolSequence"]
                }
            },
            "required": [
                "SelectionType"
            ]
        },
        "ConfigSpaceSelector": {
            "title": "ConfigSpaceSelector that takes random configuration from configuration space.",
            "description": "A Dict with random configuration.",
            "type": "object",
            "properties": {
                "SelectionType": {
                    "type": "string",
                    "enum": ["ConfigSpaceSelector"]
                }
            },
            "required": [
                "SelectionType"
            ]
        }
    }
}